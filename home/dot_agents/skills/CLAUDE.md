# skills/

Compatibility shim.

Canonical agent runtime contract lives in `AGENTS.md`. Read that first.

Claude-specific skill history/config may still mention Claude paths, but shared script execution must use `~/.agents/skills/scripts` and Pi-style `bash` / `subagent` guidance.
